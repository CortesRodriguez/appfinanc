Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Bcrypt==1.0.1
Flask-JWT-Extended==4.7.4
pandas==2.2.2
numpy==1.26.4
yfinance>=0.2.54
requests==2.32.3
python-dotenv==1.0.1
pytest==8.2.2

# RF-04.1: FinBERT es el clasificador NLP central del sistema. Si por cualquier
# motivo no se puede instalar (falta de espacio en disco, restricciones de
# despliegue), el sistema sigue funcionando con plantillas puras — pero se
# pierde la señal de sentimiento del modelo. Instalación recomendada:
transformers>=4.40
torch>=2.0
